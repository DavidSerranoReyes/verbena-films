---
import MainLayout from '../layouts/MainLayout.astro';
import Navigation from '../components/Navigation/Navigation.astro';
import Footer from '../components/Footer/Footer.astro';

const currentLang = 'es';

const translations: Record<string, Record<string, string>> = {
  es: {
    'page.title': 'Noticias | Verbena Films',
    'page.description': 'Últimas noticias de Verbena Films',
    'news.title': 'Noticias',
    'news.subtitle': 'Últimas actualizaciones y proyectos',
  },
  en: {
    'page.title': 'News | Verbena Films',
    'page.description': 'Latest news from Verbena Films',
    'news.title': 'News',
    'news.subtitle': 'Latest updates and projects',
  },
};

function t(key: string, lang: string = 'es'): string {
  return translations[lang]?.[key] || key;
}
---

<MainLayout 
  title={t('page.title', currentLang)}
  description={t('page.description', currentLang)}
>
  <Navigation currentLang={currentLang as 'es' | 'en'} />
  
  <section class="news-section">
    <div class="container">
      <h1>{t('news.title', currentLang)}</h1>
      <p>{t('news.subtitle', currentLang)}</p>
      <p>Contenido en desarrollo...</p>
    </div>
  </section>

  <Footer currentLang={currentLang as 'es' | 'en'} />
</MainLayout>

<style>
  .news-section {
    padding: var(--spacing-2xl) 0;
    background: var(--color-dark-bg);
    margin-top: var(--spacing-2xl);
    min-height: calc(100vh - var(--spacing-2xl));
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-lg);
  }

  h1 {
    font-size: 3rem;
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-lg);
  }

  p {
    font-size: 1rem;
    color: var(--color-text-secondary);
    line-height: 1.6;
  }
</style>
